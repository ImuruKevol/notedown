# 공백 없는 체크박스 문법 지원 및 저장 기준 진행률 복원

- **ID**: 003
- **날짜**: 2026-07-21
- **유형**: 버그 수정
- **리뷰 ID**: nfhzvczchcfhmybzrrdydxsjpaudedim

## 작업 요약

공백 없는 미완료 체크박스 `[]`를 체크리스트 항목으로 세지 않아 저장 후에도 완료 항목만 기준으로 100%가 표시되던 원인을 수정했다.
프리뷰 렌더링 시에만 `[]`를 표준 체크박스 문법으로 정규화해 원문을 보존하면서 체크박스와 내용을 클릭할 수 있게 했고, 사이드바 진행률은 미저장 초안이 아니라 저장된 본문을 기준으로 갱신하도록 복원했다.

## 원문 요청사항

```text
# ReviewOps Codex 작업 요청

아래 요청을 현재 프로젝트 루트에서 처리하세요. 필요한 파일을 직접 수정하고, 마지막 응답은 한국어로 간결하게 작성하세요.
스트리밍 응답은 사용하지 않습니다. 작업이 끝난 뒤 변경 요약, 확인한 내용, 남은 리스크만 정리하세요.
이 작업의 세션 단위는 아래 리뷰 ID입니다. 리뷰 ID가 같으면 같은 Codex 히스토리 맥락으로 이어서 처리하세요.

## 사용자 요청

미저장 체크리스트는 좌측 진행률에 반영될 필요는 없어.
내가 아까 확인한건 저장을 했음에도 왼쪽에 반영이 안된다는거야.
내가 체크 안한 상태는 []로 쓰고 사이에 공백이 없어서 그런건가 싶어서 확인해보니 진짜네; 이 부분만 고치면 될 것 같아.

## 리뷰 요약

- 리뷰 ID: nfhzvczchcfhmybzrrdydxsjpaudedim
- 제목: 체크박스 관련 오류
- 요청 링크: http://172.16.0.143:5500
- Codex 요청자: 권태욱
- 프로젝트 루트: /Users/ktw/Documents/notedown-server
- Codex 세션 ID: 019f8211-6bec-7e83-9f7b-ec14d7d47e9d
- Codex 모델: 5.6 sol (gpt-5.6-sol)
- Codex 추론수준: ultra (ultra)
- 스크린샷 컨텍스트: 없음
- 에이전트 작업 지시서 컨텍스트: 없음
- HTML 문서 생성 규칙 컨텍스트: 없음
- HTML 문서 설정 컨텍스트: 없음
- HTML 프로젝트 인스트럭션 파일: 없음
- 첨부파일 컨텍스트: 0개

## 세션 처리

저장된 Codex 세션을 resume해 이전 대화 맥락을 우선 사용하세요. 이전 Codex 히스토리는 이 요청에 포함되지 않습니다.
```

## 변경 파일 목록

- `src/app/component.nav.sidebar/view.ts`
  - `[]`, `[ ]`, `[x]`, `[X]`를 모두 체크리스트 문법으로 집계한다.
  - 본문 편집 이벤트는 지연 로드 보호용 pending 상태에만 기록하고 미저장 진행률은 즉시 렌더링하지 않는다.
- `src/app/page.notes/view.ts`
  - 코드 펜스 밖의 `[]`를 프리뷰 변환 단계에서만 `[ ]`로 정규화한다.
  - 공백 없는 체크박스도 프리뷰 작업 항목 판별, 클릭 토글, 완료 상태 판별에 포함한다.
- `devlog.md`
- `devlog/2026-07-21/003-compact-checkbox-syntax.md`

## 검증 결과

- WIZ normal build 성공: `wiz_project_build(projectName="main", clean=false)`.
- Playwright에서 저장된 `- [] one`, `- [] two`, `- [x] three`가 사이드바 33%와 프리뷰 체크박스 3개로 표시되는 것을 확인했다.
- 프리뷰 체크박스 클릭 시 에디터 원문이 `[]`에서 `[x]`로 바뀌지만 미저장 상태의 사이드바는 33%를 유지하는 것을 확인했다.
- 프리뷰 내용 클릭도 동일하게 에디터에 반영되고, 저장 후 사이드바가 변경된 저장 본문 기준 100%로 갱신되는 것을 확인했다.
- UI 검증에 `season-wiz-project=main`, `season-wiz-devmode=true` 쿠키를 적용했다.
- 정적 검증 서버의 콘솔 오류는 백엔드가 없는 환경의 `/auth/check` HTTP 501뿐이며 체크박스 관련 런타임 오류는 없었다.
- `git diff --check` 통과.

## 남은 리스크

- 실제 Electron 파일 저장 API 대신 WIZ 빌드 산출물의 localStorage 저장 경로로 동작을 검증했다.
- Electron 배포 패키지는 이번 수정 범위에서 재생성하지 않았다.
