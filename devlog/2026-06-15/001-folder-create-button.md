# 폴더 패널 새 폴더 버튼 동작 복구

- **ID**: 001
- **날짜**: 2026-06-15
- **유형**: 버그 수정

## 작업 요약

폴더 패널 상단의 새 폴더 버튼에 클릭 핸들러가 없어 아무 동작도 하지 않던 문제를 수정했다.
새 폴더 이름을 입력하면 폴더 목록에 즉시 추가되고, 선택된 빈 폴더 상태와 이후 새 노트 생성 흐름이 같은 작업공간을 기준으로 동작하도록 보강했다.

## 원문 요청사항

```text
# ReviewOps Codex 작업 요청

아래 요청을 현재 프로젝트 루트에서 처리하세요. 필요한 파일을 직접 수정하고, 마지막 응답은 한국어로 간결하게 작성하세요.
스트리밍 응답은 사용하지 않습니다. 작업이 끝난 뒤 변경 요약, 확인한 내용, 남은 리스크만 정리하세요.
이 작업의 세션 단위는 아래 리뷰 ID입니다. 리뷰 ID가 같으면 같은 Codex 히스토리 맥락으로 이어서 처리하세요.

## 사용자 요청

작업 시작

## 리뷰 요약

- 리뷰 ID: xnopyinpvusibqdykzjjxnnzxwksqhrc
- 제목: 폴더 생성 동작 안함
- 요청 링크: http://172.16.0.143:3009
- Codex 요청자: 권태욱
- 프로젝트 루트: /Users/ktw/Documents/notedown
- Codex 세션 ID: 신규
- 스크린샷 컨텍스트: 없음
- 에이전트 작업 지시서 컨텍스트: 포함됨
- HTML 문서 생성 규칙 컨텍스트: 없음
- HTML 문서 설정 컨텍스트: 없음
- HTML 프로젝트 인스트럭션 파일: 없음
- 첨부파일 컨텍스트: 0개

## 리뷰어 요청 내용

기본 화면에서 왼쪽 아래에 폴더 목록 보는 아이콘 버튼을 클릭하면 "모든 노트"를 포함한 폴더 목록이 뜸. 근데 그 위에 있는 폴더 생성 + 버튼이 동작하지 않음.
```

## 변경 파일 목록

- `src/app/component.nav.sidebar/view.pug`
  - 폴더 패널 상단의 새 폴더 버튼에 `createFolder($event)` 클릭 핸들러를 연결했다.
- `src/app/component.nav.sidebar/view.ts`
  - 새 폴더 이름 입력, 중복 방지, 폴더 ID 생성, `localStorage` 기반 폴더 목록 저장을 추가했다.
  - 빈 폴더 선택 시 현재 노트 선택을 해제해 노트 목록과 본문 상태가 어긋나지 않도록 했다.
- `src/app/layout.sidebar/view.ts`
  - 커맨드 팔렛트와 레이아웃의 작업공간 목록이 저장된 빈 폴더도 인식하도록 보강했다.
- `src/app/page.notes/view.ts`
  - 빈 폴더 선택 이벤트로 현재 노트를 비울 수 있게 하고, 새 노트 생성 시 현재 작업공간을 사용하도록 했다.
- `devlog.md`, `devlog/2026-06-15/001-folder-create-button.md`
  - 작업 요약 및 상세 이력을 추가했다.

## 검증 결과

- `wiz_project_build(clean=false)` 성공.
- 제공된 리뷰 URL `http://172.16.0.143:3009`는 현재 환경에서 연결되지 않아 실제 브라우저 클릭 검증은 수행하지 못했다.
- Browser 플러그인의 in-app browser 세션도 현재 `iab`를 사용할 수 없어 대체 브라우저 검증을 수행하지 못했다.

## 남은 리스크

- 새 폴더는 우선 브라우저 `localStorage`에 저장된다. 폴더 안에 노트를 만들면 기존 노트 저장 흐름을 통해 해당 작업공간 정보가 함께 유지된다.
